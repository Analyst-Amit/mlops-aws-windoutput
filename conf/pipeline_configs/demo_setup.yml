# Delete MLflow Registry model
# Model name set in deployment.yml
delete_model_registry: True

# Delete MLflow Tracking experiments (both the training experiment and the deployment experiment)
# Experiment paths/id set in deployment.yml
delete_mlflow_experiments: True
